---
description: Prioritize MCP tools over CLI for integrated environments
globs: **/*
alwaysApply: true
---

**For Cursor/IDE Integration:**
- Use MCP tools (better performance, structured data)
- CLI as fallback only
- AI-powered tools: `parse_prd`, `expand_task`, `update_subtask`, `research`
- Restart MCP server if core logic changes

**Configuration:**
- API keys in `.cursor/mcp.json` env section (MCP)
- API keys in `.env` file (CLI)
- Model selection via `models` command/tool