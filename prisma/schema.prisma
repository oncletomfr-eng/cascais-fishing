generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([provider, providerAccountId])
  @@map("accounts")
}

model EventSkillCriteria {
  id              String             @id @default(cuid())
  tripId          String
  criteriaType    SkillCriteriaType
  minimumValue    Int?
  requiredSkills  FishingTechnique[] @default([])
  requiredSpecies FishSpecies[]      @default([])
  description     String?
  isRequired      Boolean            @default(true)
  weight          Int                @default(1)
  createdAt       DateTime           @default(now())
  trip            GroupTrip          @relation(fields: [tripId], references: [id], onDelete: Cascade)

  @@map("event_skill_criteria")
}

model Achievement {
  id                String            @id @default(cuid())
  type              AchievementType   @unique
  name              String
  description       String
  icon              String
  category          BadgeCategory
  rarity            AchievementRarity @default(COMMON)
  maxProgress       Int               @default(1) // Максимальный прогресс для разблокировки
  progressStep      Int               @default(1) // Шаг уведомлений о прогрессе
  lockedVisible     Boolean           @default(true) // Видимо ли заблокированное достижение
  lockedDescVisible Boolean           @default(false) // Видимо ли описание заблокированного
  isActive          Boolean           @default(true)
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  
  // Связь с пользователями через UserAchievement
  userAchievements UserAchievement[]

  @@map("achievements")
}

model UserAchievement {
  id            String    @id @default(cuid())
  userId        String
  achievementId String
  progress      Int       @default(0) // Текущий прогресс
  unlocked      Boolean   @default(false) // Разблокировано ли
  unlockedAt    DateTime? // Когда разблокировано
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  // Связи
  user        User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  achievement Achievement @relation(fields: [achievementId], references: [id], onDelete: Cascade)

  @@unique([userId, achievementId])
  @@map("user_achievements")
}

model FisherBadge {
  id            String            @id @default(cuid())
  profileId     String
  name          String
  description   String?
  icon          String
  category      BadgeCategory     @default(ACHIEVEMENT)
  rarity        AchievementRarity @default(COMMON)
  requiredValue Int?
  earnedAt      DateTime          @default(now())
  fisherProfile FisherProfile     @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@map("fisher_badges")
}

model FisherProfile {
  id                    String             @id @default(cuid())
  userId                String             @unique
  experienceLevel       FishingExperience  @default(BEGINNER)
  specialties           FishingSpecialty[]
  bio                   String?
  rating                Decimal            @default(5.0) @db.Decimal(2, 1)
  completedTrips        Int                @default(0)
  createdTrips          Int                @default(0) // Созданных событий
  reliability           Decimal            @default(100.0) @db.Decimal(5, 2)
  totalReviews          Int                @default(0)
  positiveReviews       Int                @default(0) // Положительные отзывы
  level                 Int                @default(1) // Уровень рыболова
  experiencePoints      Int                @default(0) // Очки опыта
  activeDays            Int                @default(0) // Активные дни
  activeDaysConsecutive Int                @default(0) // Последовательные активные дни
  totalFishCaught       Int                @default(0) // Всего поймано рыбы (кг)
  uniqueSpecies         String[]           @default([]) // Уникальные виды рыб
  favoriteLocation      String? // Любимое место
  isActive              Boolean            @default(true)
  lastActiveAt          DateTime           @default(now())
  country               String?
  city                  String?
  latitude              Decimal?           @db.Decimal(10, 8) // Для географических достижений
  longitude             Decimal?           @db.Decimal(11, 8)
  secretCode            String? // Секретный код для входа
  
  // 🎯 Расширенная система репутации
  // Experience - детальная статистика опыта
  experienceStats Json? // ExperienceStats: biggestCatch, techniques, seasonal activity
  
  // Social - социальные рейтинги
  mentorRating      Decimal @default(5.0) @db.Decimal(2, 1) // Как учит новичков
  teamworkRating    Decimal @default(5.0) @db.Decimal(2, 1) // Работа в команде
  reliabilityRating Decimal @default(5.0) @db.Decimal(2, 1) // Надежность
  respectRating     Decimal @default(5.0) @db.Decimal(2, 1) // Соблюдение правил
  reviewBreakdown   Json? // Детальная разбивка отзывов по оценкам
  
  // Certificates & Achievements - сертификаты и достижения
  certificates       Json? // Certificate[]: профессиональные сертификаты
  fishingPreferences Json? // FishingPreferences: предпочтения рыболова
  
  // Catch Records - записи уловов
  totalWeightCaught   Decimal  @default(0.0) @db.Decimal(10, 2) // Общий вес улова (кг)
  averageTripDuration Decimal? @db.Decimal(5, 2) // Средняя продолжительность поездки (часы)
  successRate         Decimal  @default(0.0) @db.Decimal(5, 2) // Процент успешных поездок
  catchRecords        Json? // CatchRecord[]: значимые уловы
  
  // Technique Skills - навыки техник
  techniqueSkills Json? // TechniqueSkill[]: опыт с разными техниками
  
  // Seasonal & Location Data  
  seasonalActivity  Json? // Активность по сезонам
  favoriteLocations String[] @default([]) // Любимые места рыбалки
  fishingZones      String[] @default([]) // Зоны где рыбачил

  // Privacy Controls for Rankings
  leaderboardVisibility    LeaderboardVisibility @default(PUBLIC) // Видимость в лидербордах
  profileVisibility        ProfileVisibility     @default(PUBLIC) // Видимость профиля
  showInCompetitions      Boolean               @default(true)   // Участие в соревнованиях
  showRankingHistory      Boolean               @default(true)   // Показывать историю рангов
  allowAchievementSharing Boolean               @default(true)   // Разрешить делиться достижениями
  anonymousMode           Boolean               @default(false)  // Анонимный режим
  visibleToFriendsOnly    Boolean               @default(false)  // Видимость только друзьям
  competitionOptOuts      String[]              @default([])     // Исключения из соревнований по ID

  createdAt DateTime      @default(now())
  updatedAt DateTime      @updatedAt
  badges    FisherBadge[]
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("fisher_profiles")
}

model GroupBooking {
  id              String        @id @default(cuid())
  tripId          String
  participants    Int           @default(1)
  totalPrice      Decimal       @db.Decimal(10, 2)
  contactName     String
  contactPhone    String
  contactEmail    String?
  status          BookingStatus @default(PENDING)
  paymentStatus   String        @default("pending")
  specialRequests String?
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  userId          String?
  trip            GroupTrip     @relation(fields: [tripId], references: [id], onDelete: Cascade)
  user            User?         @relation(fields: [userId], references: [id])

  @@index([tripId])
  @@index([userId])
  @@map("group_bookings")
}

model GroupTrip {
  id                   String                  @id @default(cuid())
  date                 DateTime
  timeSlot             TimeSlot
  maxParticipants      Int                     @default(8)
  minRequired          Int                     @default(6)
  pricePerPerson       Decimal                 @default(95.00) @db.Decimal(10, 2)
  status               GroupTripStatus         @default(FORMING)
  description          String?
  meetingPoint         String?
  specialNotes         String?
  createdAt            DateTime                @default(now())
  updatedAt            DateTime                @updatedAt
  captainId            String?
  approvalMode         ParticipantApprovalMode @default(MANUAL)
  departureLocation    String?
  difficultyRating     Int                     @default(3)
  equipment            EquipmentType           @default(PROVIDED)
  estimatedFishCatch   Int?
  eventType            FishingEventType        @default(COMMERCIAL)
  fishingTechniques    FishingTechnique[]      @default([])
  fishingZones         String[]                @default([])
  maxGroupSize         Int?
  minimumWeatherScore  Int                     @default(6)
  recommendedFor       String[]                @default([])
  skillLevel           SkillLevelRequired      @default(ANY)
  socialMode           SocialEventMode         @default(COLLABORATIVE)
  targetSpecies        FishSpecies[]           @default([])
  weatherDependency    Boolean                 @default(true)
  skillCriteria        EventSkillCriteria[]
  bookings             GroupBooking[]
  captain              User?                   @relation(fields: [captainId], references: [id])
  participantApprovals ParticipantApproval[]
  reviews              Review[]
  payments             Payment[]
  
  // Умные рекомендации
  recommendations SmartRecommendation[] // Рекомендации этой поездки
  similarityAsTrip1 TripSimilarity[] @relation("Trip1Similarities") // Сходства где эта поездка первая
  similarityAsTrip2 TripSimilarity[] @relation("Trip2Similarities") // Сходства где эта поездка вторая

  @@map("group_trips")
}

model ParticipantApproval {
  id             String         @id @default(cuid())
  tripId         String
  participantId  String
  message        String?
  status         ApprovalStatus @default(PENDING)
  approvedBy     String?
  rejectedReason String?
  processedAt    DateTime?
  appliedAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  participant    User           @relation(fields: [participantId], references: [id], onDelete: Cascade)
  trip           GroupTrip      @relation(fields: [tripId], references: [id], onDelete: Cascade)

  @@unique([tripId, participantId])
  @@map("participant_approvals")
}

model PrivateBooking {
  id              String        @id @default(cuid())
  date            DateTime
  timeSlot        TimeSlot
  participants    Int           @default(1)
  contactName     String
  contactPhone    String
  contactEmail    String?
  totalPrice      Decimal       @default(400.00) @db.Decimal(10, 2)
  status          BookingStatus @default(PENDING)
  specialRequests String?
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@map("private_bookings")
}

model Review {
  id         String    @id @default(cuid())
  tripId     String
  fromUserId String
  toUserId   String
  rating     Int
  comment    String?
  verified   Boolean   @default(false)
  helpful    Int       @default(0)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  fromUser   User      @relation("UserFromReviews", fields: [fromUserId], references: [id], onDelete: Cascade)
  toUser     User      @relation("UserToReviews", fields: [toUserId], references: [id], onDelete: Cascade)
  trip       GroupTrip @relation(fields: [tripId], references: [id], onDelete: Cascade)

  @@unique([tripId, fromUserId, toUserId])
  @@map("reviews")
}

model Session {
  sessionToken String   @unique
  userId       String
  expires      DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model SystemSetting {
  id          String   @id @default(cuid())
  key         String   @unique
  value       String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime

  @@map("system_settings")
}

model User {
  id                   String                @id @default(cuid())
  name                 String?
  email                String                @unique
  emailVerified        DateTime?
  image                String?
  password             String?               // For credentials provider
  role                 UserRole              @default(PARTICIPANT)
  
  // Stripe integration
  stripeCustomerId     String?               @unique // Stripe Customer ID
  
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  accounts             Account[]
  fisherProfile        FisherProfile?
  groupBookings        GroupBooking[]
  captainedTrips       GroupTrip[]
  participantApprovals ParticipantApproval[]
  reviewsFrom          Review[]              @relation("UserFromReviews")
  reviewsTo            Review[]              @relation("UserToReviews")
  sessions             Session[]
  achievements         UserAchievement[] // Достижения пользователя
  
  // Монетизация
  subscriptions     Subscription[]
  payments          Payment[]
  paymentMethods    PaymentMethod[]    // Сохраненные методы платежа
  courseEnrollments CourseEnrollment[]
  advertisements    Advertisement[]
  
  // Морской календарь
  catchRecords CatchRecord[] // Записи об уловах

  // Цифровой дневник рыболова
  diaryEntries FishingDiaryEntry[] // Записи дневника

  // Умные рекомендации
  recommendationsReceived SmartRecommendation[] @relation("UserRecommendations") // Полученные рекомендации
  recommendationsFromCaptain SmartRecommendation[] @relation("CaptainRecommendations") // Рекомендации как капитан
  recommendationInteractions RecommendationInteraction[] // Взаимодействия с рекомендациями
  captainRecommendations CaptainRecommendation[] @relation("CaptainRecommendationsAuthored") // Авторские рекомендации капитана
  captainRecommendationVotes CaptainRecommendationVote[] // Голоса за рекомендации капитанов
  
  // Система выплат
  payouts Payout[] @relation("CaptainPayouts") // Выплаты капитану
  payoutSchedule PayoutSchedule? @relation("CaptainPayoutSchedule") // График выплат капитана
  
  // Налоговая отчетность
  taxDocuments TaxDocument[] @relation("CaptainTaxDocuments") // Налоговые документы капитана
  
  // Competition system
  createdCompetitions Competition[] @relation("CompetitionCreator") // Созданные соревнования  
  competitionParticipations CompetitionParticipant[] @relation("CompetitionParticipants") // Участие в соревнованиях
  competitionRankings CompetitionRanking[] @relation("CompetitionRankings") // Рейтинги в соревнованиях
  
  // Seasonal competition system
  createdSeasons Season[] @relation("SeasonCreator") // Созданные сезоны
  seasonParticipations SeasonParticipant[] @relation("SeasonParticipants") // Участие в сезонных соревнованиях
  archivedSeasons SeasonArchive[] @relation("SeasonArchivers") // Архивированные сезоны
  createdCompetitionEvents CompetitionEvent[] @relation("CompetitionEventCreator") // Созданные события соревнований
  createdSeasonAnnouncements SeasonAnnouncement[] @relation("SeasonAnnouncementCreator") // Созданные объявления сезонов

  // Reward distribution system
  rewardDistributions RewardDistribution[] // Полученные награды
  rewardInventory RewardInventory[] // Инвентарь наград
  wonRewardEvents RewardEvent[] @relation("RewardEventWinner") // События где был победителем

  @@map("users")
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@id([identifier, token])
  @@map("verification_tokens")
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum BadgeCategory {
  ACHIEVEMENT
  MILESTONE
  SPECIAL
  SEASONAL
  FISH_SPECIES // Достижения по видам рыб
  TECHNIQUE // Достижения по техникам
  SOCIAL // Социальные достижения
  GEOGRAPHY // Географические достижения
}

enum CompetitionCategory {
  MONTHLY_CHAMPIONS
  BIGGEST_CATCH
  MOST_ACTIVE
  BEST_MENTOR
  TECHNIQUE_MASTER
  SPECIES_SPECIALIST
  CONSISTENCY_KING
  ROOKIE_OF_MONTH
  VETERAN_ANGLER
  SOCIAL_BUTTERFLY
}

enum CompetitionType {
  SEASONAL
  MONTHLY
  WEEKLY
  ONGOING
  SPECIAL_EVENT
}

enum CompetitionStatus {
  ACTIVE
  COMPLETED
  UPCOMING
  CANCELLED
}

enum SeasonType {
  WEEKLY
  MONTHLY
  QUARTERLY
  YEARLY
  CUSTOM
}

enum SeasonStatus {
  UPCOMING
  ACTIVE
  COMPLETED
  CANCELLED
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum BookingType {
  PRIVATE
  GROUP
}

enum EquipmentType {
  PROVIDED
  BRING_OWN
  RENTAL_AVAILABLE
  PARTIALLY_PROVIDED
}

enum FishSpecies {
  SEABASS
  DORADO
  SEABREAM
  MACKEREL
  SARDINE
  TUNA
  ALBACORE
  BONITO
  SWORDFISH
  MAHI_MAHI
  BLUE_MARLIN
  WHITE_MARLIN
  SAILFISH
  GROUPER
  RED_SNAPPER
  JOHN_DORY
  SOLE
  TURBOT
  AMBERJACK
  CONGER_EEL
  OCTOPUS
  CUTTLEFISH
  MIXED_SPECIES
}

enum FishingEventType {
  COMMERCIAL
  COMMUNITY
  TOURNAMENT
  LEARNING
}

enum FishingExperience {
  BEGINNER
  INTERMEDIATE
  EXPERT
}

enum FishingSpecialty {
  DEEP_SEA
  SHORE
  FLY_FISHING
  SPORT_FISHING
}

enum FishingTechnique {
  TROLLING
  JIGGING
  BOTTOM_FISHING
  FLY_FISHING
  SPINNING
  SURFCASTING
  DRIFTING
  POPPING
  LIVE_BAIT
  DEEP_DROP
}

enum GroupTripStatus {
  FORMING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum ParticipantApprovalMode {
  AUTO
  MANUAL
  SKILL_BASED
  HYBRID
}

enum SkillCriteriaType {
  MINIMUM_EXPERIENCE
  COMPLETED_TRIPS
  SPECIES_EXPERIENCE
  TECHNIQUE_SKILL
  CERTIFICATION
  EQUIPMENT_OWNERSHIP
  RATING_THRESHOLD
  LANGUAGE_REQUIREMENT
}

enum SkillLevelRequired {
  BEGINNER
  INTERMEDIATE
  ADVANCED
  EXPERT
  ANY
}

enum SocialEventMode {
  COMPETITIVE
  COLLABORATIVE
  EDUCATIONAL
  RECREATIONAL
  FAMILY
}

enum TimeSlot {
  MORNING_9AM
  AFTERNOON_2PM
}

enum UserRole {
  PARTICIPANT
  CAPTAIN
  ADMIN
}

enum AchievementRarity {
  COMMON // Обычное (легко получить)
  UNCOMMON // Необычное
  RARE // Редкое
  EPIC // Эпическое
  LEGENDARY // Легендарное
  MYTHIC // Мифическое (самое редкое)
}

enum AchievementType {
  // Виды рыб
  TUNA_MASTER // Мастер тунца (поймал 10+ тунцов)
  DORADO_HUNTER // Охотник на дорадо (поймал 5+ дорадо)
  SEABASS_EXPERT // Эксперт морского окуня
  MARLIN_LEGEND // Легенда марлина
  SPECIES_COLLECTOR // Коллекционер видов (поймал 5+ разных видов)
  
  // Техники
  TROLLING_EXPERT // Троллинг-эксперт (10+ поездок с троллингом)
  JIGGING_MASTER // Джиг-мастер
  BOTTOM_FISHING_PRO // Профи донной ловли
  FLY_FISHING_ARTIST // Художник нахлыста
  TECHNIQUE_VERSATILE // Универсал техник (использовал 4+ техники)
  
  // Социальные
  NEWBIE_MENTOR // Наставник новичков (помог 5+ новичкам)
  GROUP_ORGANIZER // Организатор групп (создал 10+ событий)
  COMMUNITY_BUILDER // Строитель сообщества
  REVIEW_MASTER // Мастер отзывов (оставил 20+ отзывов)
  RELIABLE_FISHER // Надежный рыболов (100% посещаемость)
  
  // География
  REEF_EXPLORER // Исследователь рифов
  DEEP_SEA_ADVENTURER // Глубоководный авантюрист
  COASTAL_SPECIALIST // Специалист прибрежной ловли
  WORLD_TRAVELER // Путешественник (рыбачил в 3+ местах)
  LOCAL_EXPERT // Местный эксперт (50+ поездок в одном месте)
}

// ============================================================================
// МОНЕТИЗАЦИЯ И БИЗНЕС-ЛОГИКА
// ============================================================================

// Подписки для капитанов (Premium €50/месяц)
model Subscription {
  id     String @id @default(cuid())
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  // Stripe данные
  stripeCustomerId     String? @unique
  stripeSubscriptionId String? @unique
  stripePriceId        String?
  
  // Подписка
  tier   SubscriptionTier   @default(FREE)
  status SubscriptionStatus @default(INACTIVE)
  
  // Даты
  currentPeriodStart DateTime?
  currentPeriodEnd   DateTime?
  cancelAtPeriodEnd  Boolean   @default(false)
  
  // Метаданные
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Связи
  payments Payment[]
  
  @@map("subscriptions")
}

enum SubscriptionTier {
  FREE
  CAPTAIN_PREMIUM // €50/месяц с расширенными возможностями
}

enum SubscriptionStatus {
  INACTIVE
  ACTIVE
  PAST_DUE
  CANCELED
  UNPAID
}

// Платежи и комиссии
model Payment {
  id String @id @default(cuid())
  
  // Связи
  userId String?
  user   User?   @relation(fields: [userId], references: [id], onDelete: SetNull)
  
  subscriptionId String?
  subscription   Subscription? @relation(fields: [subscriptionId], references: [id], onDelete: SetNull)
  
  tripId String?
  trip   GroupTrip? @relation(fields: [tripId], references: [id], onDelete: SetNull)
  
  // Stripe данные
  stripePaymentId String? @unique
  stripeInvoiceId String? @unique
  
  // Payment method used for this payment
  paymentMethodId String?
  paymentMethod   PaymentMethod? @relation(fields: [paymentMethodId], references: [id], onDelete: SetNull)
  
  // Платеж
  type     PaymentType
  amount   Int // В центах
  currency String        @default("EUR")
  status   PaymentStatus @default(PENDING)
  
  // Комиссии (для туров)
  commissionAmount Int? // Комиссия платформы в центах
  commissionRate   Float? // Процент комиссии (15-20%)
  
  // Метаданные
  description String?
  metadata    Json?
  
  // Даты
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  paidAt    DateTime?
  
  // Связь с диспутами
  disputes  PaymentDispute[]
  
  @@map("payments")
}

enum PaymentType {
  SUBSCRIPTION // Подписка капитана
  TOUR_BOOKING // Бронирование тура
  COURSE_PURCHASE // Покупка курса
  ADVERTISING // Реклама в ленте
}

enum PaymentStatus {
  PENDING
  SUCCEEDED
  FAILED
  CANCELED
  REFUNDED
  REQUIRES_ACTION
}

// Webhook Event Logs для мониторинга и отладки
model WebhookEventLog {
  id            String   @id @default(cuid())
  eventType     String   // Тип Stripe события
  eventId       String   @unique // ID события от Stripe
  status        WebhookStatus @default(RECEIVED)
  attempts      Int      @default(0)
  maxAttempts   Int      @default(3)
  lastError     String?  // Последняя ошибка
  processingTime Int?    // Время обработки в мс
  metadata      Json?    // Дополнительные данные
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  @@map("webhook_event_logs")
}

enum WebhookStatus {
  RECEIVED
  PROCESSING
  SUCCESS
  FAILED
  RETRYING
}

// Отслеживание диспутов по платежам
model PaymentDispute {
  id               String   @id @default(cuid())
  stripeDisputeId  String   @unique
  chargeId         String   // Stripe Charge ID
  paymentId        String?  // Связь с нашим платежом
  amount           Int      // Сумма диспута в центах
  reason           String   // Причина диспута
  status           String   // Статус диспута в Stripe
  evidence         Json?    // Доказательства (если есть)
  metadata         Json?    // Метаданные от Stripe
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  
  // Связь с платежом (если найден)
  payment          Payment? @relation(fields: [paymentId], references: [id], onDelete: SetNull)
  
  @@map("payment_disputes")
}

// Сохраненные методы платежа пользователей
model PaymentMethod {
  id                 String             @id @default(cuid())
  userId             String             // ID пользователя
  stripePaymentMethodId String          @unique // Stripe Payment Method ID
  
  // Основная информация
  type               PaymentMethodType  @default(CARD)
  isDefault          Boolean            @default(false)
  
  // Card данные (PCI compliant - только последние 4 цифры и бренд)
  cardLast4          String?
  cardBrand          String?            // visa, mastercard, etc.
  cardExpMonth       Int?
  cardExpYear        Int?
  cardFingerprint    String?            // Unique identifier for the card
  
  // Billing address данные
  billingName        String?
  billingEmail       String?
  billingCountry     String?
  billingCity        String?
  billingPostalCode  String?
  
  // Metadata и статус
  status             PaymentMethodStatus @default(ACTIVE)
  metadata           Json?               // Дополнительные данные от Stripe
  
  // Timestamps
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  lastUsedAt         DateTime?           // Когда последний раз использовался
  
  // Связи
  user               User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  payments           Payment[]           // Платежи сделанные этим методом
  payouts            Payout[]            // Выплаты через этот способ
  payoutSchedules    PayoutSchedule[]    // Настройки графика выплат
  
  // Индексы для оптимизации
  @@index([userId])
  @@index([userId, isDefault])
  @@map("payment_methods")
}

enum PaymentMethodType {
  CARD
  SEPA_DEBIT
  IDEAL
  BANCONTACT
  GIROPAY
  SOFORT
  PAYPAL
}

enum PaymentMethodStatus {
  ACTIVE
  INACTIVE
  EXPIRED
  FAILED
}

// Сертификационные курсы онлайн
model Course {
  id String @id @default(cuid())
  
  // Информация о курсе
  title       String
  description String?
  category    CourseCategory
  
  // Цена
  price         Int // В центах
  stripePriceId String? @unique
  
  // Контент
  content    Json? // Структурированный контент курса
  duration   Int? // Продолжительность в минутах
  difficulty CourseDifficulty @default(BEGINNER)
  
  // Статус
  published Boolean @default(false)
  
  // Метаданные
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Связи
  enrollments CourseEnrollment[]
  
  @@map("courses")
}

enum CourseCategory {
  BASIC_FISHING // Основы рыбалки
  ADVANCED_TECHNIQUES // Продвинутые техники  
  CAPTAIN_LICENSE // Лицензия капитана
  SAFETY // Безопасность
  EQUIPMENT // Снаряжение
}

enum CourseDifficulty {
  BEGINNER
  INTERMEDIATE
  ADVANCED
  EXPERT
}

// Записи на курсы
model CourseEnrollment {
  id String @id @default(cuid())
  
  // Связи
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  courseId String
  course   Course @relation(fields: [courseId], references: [id], onDelete: Cascade)
  
  // Прогресс
  status   EnrollmentStatus @default(ENROLLED)
  progress Float            @default(0) // 0-100%
  score    Float? // Итоговая оценка
  
  // Даты
  enrolledAt  DateTime  @default(now())
  startedAt   DateTime?
  completedAt DateTime?
  
  // Сертификат
  certificateIssued Boolean @default(false)
  certificateUrl    String?
  
  @@unique([userId, courseId])
  @@map("course_enrollments")
}

enum EnrollmentStatus {
  ENROLLED
  IN_PROGRESS
  COMPLETED
  DROPPED
}

// Реклама в ленте событий
model Advertisement {
  id String @id @default(cuid())
  
  // Рекламодатель
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  // Контент рекламы
  title       String
  description String?
  imageUrl    String?
  targetUrl   String?
  
  // Настройки показа
  type     AdType     @default(EQUIPMENT_PROMOTION)
  priority AdPriority @default(NORMAL)
  
  // Таргетинг
  targetAudience Json? // Настройки таргетинга
  locations      String[] // Географические области
  
  // Бюджет и статистика
  budget       Int // В центах
  costPerClick Int? // Цена за клик в центах
  impressions  Int  @default(0)
  clicks       Int  @default(0)
  conversions  Int  @default(0)
  
  // Статус и даты
  status    AdStatus  @default(DRAFT)
  startDate DateTime?
  endDate   DateTime?
  
  // Метаданные
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("advertisements")
}

enum AdType {
  EQUIPMENT_PROMOTION // Реклама снастей/оборудования
  FEATURED_LISTING // Выделенное объявление
  SPONSOR_MESSAGE // Спонсорское сообщение
}

enum AdPriority {
  LOW
  NORMAL
  HIGH
  PREMIUM
}

enum AdStatus {
  DRAFT
  PENDING_REVIEW
  ACTIVE
  PAUSED
  COMPLETED
  REJECTED
}

// ============================================================================
// МОРСКОЙ КАЛЕНДАРЬ И АСТРОНОМИЧЕСКИЕ ДАННЫЕ
// ============================================================================

// Лунные фазы и их влияние на рыбалку
model LunarPhase {
  id               String         @id @default(cuid())
  date             DateTime // Дата фазы
  type             LunarPhaseType
  angle            Float // Угол фазы (0-360°)
  illumination     Float // Процент освещенности (0-100)
  distanceKm       Float // Расстояние до Луны в км
  apparentDiameter Float // Видимый диаметр в градусах
  
  // Китайский лунный календарь
  chineseLunarData Json? // ChineseLunarDay данные
  
  // Влияние на рыбалку
  fishingInfluence Json? // LunarInfluence данные
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Связи
  fishingConditions FishingConditions[]
  catchRecords      CatchRecord[]
  
  @@unique([date])
  @@map("lunar_phases")
}

enum LunarPhaseType {
  NEW_MOON
  WAXING_CRESCENT
  FIRST_QUARTER
  WAXING_GIBBOUS
  FULL_MOON
  WANING_GIBBOUS
  LAST_QUARTER
  WANING_CRESCENT
}

// Прогноз рыбалки на день
model FishingConditions {
  id       String   @id @default(cuid())
  date     DateTime // Дата прогноза
  location Json // FishingLocation данные
  
  // Общие условия
  overallRating Int // Общая оценка клёва (1-10)
  lunarPhaseId  String
  lunarPhase    LunarPhase @relation(fields: [lunarPhaseId], references: [id])
  
  // Влияния
  tidalInfluence Json? // TidalInfluence данные
  weatherImpact  Json? // Влияние погоды на клёв
  
  // Рекомендации
  bestHours       Json? // TimeWindow[] лучшие часы
  recommendations String[] // Рекомендации
  
  // Виды рыб
  speciesInfluence Json? // SpeciesFishingInfluence[] данные
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([date, location])
  @@map("fishing_conditions")
}

// Исторические данные об уловах
model CatchRecord {
  id String @id @default(cuid())
  
  // Основная информация
  date     DateTime // Дата улова
  location Json // FishingLocation данные
  anglerId String? // Рыболов (может быть null для анонимных данных)
  angler   User?    @relation(fields: [anglerId], references: [id])
  
  // Лунные условия
  lunarPhaseId String
  lunarPhase   LunarPhase @relation(fields: [lunarPhaseId], references: [id])
  
  // Улов
  catches     Json // CatchDetails[] пойманные рыбы
  totalWeight Float // Общий вес улова в кг
  totalCount  Int // Общее количество рыбы
  
  // Условия
  weatherData Json? // WeatherConditions данные
  tackleUsed  Json? // TackleUsed[] использованные снасти
  techniques  FishingTechnique[] // Использованные техники
  
  // Дополнительно
  duration Int? // Продолжительность рыбалки в минутах
  success  Boolean @default(true) // Успешная ли рыбалка
  notes    String? // Заметки
  
  // Проверка и качество данных
  verified   Boolean         @default(false) // Проверенные данные
  dataSource CatchDataSource @default(USER_REPORT) // Источник данных
  confidence Float           @default(1.0) // Уверенность в данных (0-1)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([date])
  @@index([lunarPhaseId])
  @@index([anglerId])
  @@map("catch_records")
}

enum CatchDataSource {
  USER_REPORT // Отчет пользователя
  CAPTAIN_LOG // Журнал капитана  
  AUTOMATED // Автоматические данные (сенсоры)
  HISTORICAL // Исторические архивы
  RESEARCH // Научные исследования
}

// Детализация видов рыб (расширение существующего enum)
model FishSpeciesDetail {
  id      String      @id @default(cuid())
  species FishSpecies @unique // Связь с существующим enum
  
  // Названия
  nameRu         String
  nameEn         String
  scientificName String
  
  // Характеристики
  seasonality      Json // SeasonalAvailability данные
  migrationPattern Json? // MigrationPattern данные
  depthPreferences Json // DepthRange данные
  temperatureRange Json // TemperatureRange данные
  
  // Рыбалка
  preferredTechniques FishingTechnique[]
  bestLunarPhases     LunarPhaseType[]
  difficulty          Int                @default(5) // Сложность ловли (1-10)
  
  // Биология
  averageSize    Json? // Размеры {min, max, average} в см
  averageWeight  Json? // Вес {min, max, average} в кг
  spawningPeriod Json? // Период нереста
  feedingHabits  Json? // Питание и поведение
  
  // Коммерческая ценность
  marketValue Float? // Рыночная стоимость за кг
  gameValue   Int? // Спортивная ценность (1-10)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("fish_species_details")
}

// Миграционные события
model MigrationEvent {
  id      String      @id @default(cuid())
  species FishSpecies // Вид рыбы
  
  // Событие
  eventType   MigrationEventType
  date        DateTime // Дата события
  probability Float // Вероятность события (0-1)
  
  // Географически
  location  Json // Местоположение события
  direction String? // Направление миграции
  depth     Float? // Глубина обнаружения
  
  // Условия
  waterTemperature Float? // Температура воды
  currentStrength  Float? // Сила течения
  
  // Источник данных
  dataSource String // Источник информации
  confidence Float  @default(0.8) // Уверенность в прогнозе

  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([species])
  @@index([date])
  @@map("migration_events")
}

enum MigrationEventType {
  ARRIVAL // Прибытие в регион
  PEAK // Пик активности
  DEPARTURE // Отбытие из региона
}

// Лучшие места для рыбалки
model FishingHotspot {
  id String @id @default(cuid())
  
  // Локация
  name              String
  latitude          Decimal @db.Decimal(10, 8)
  longitude         Decimal @db.Decimal(11, 8)
  depth             Float? // Средняя глубина
  distanceFromShore Float? // Расстояние от берега в км
  
  // Характеристики
  bottomType    String? // Тип дна
  structureType String? // Тип структуры (риф, затонувший корабль и т.д.)
  targetSpecies FishSpecies[] // Основные виды рыб
  
  // Условия
  bestSeasons     Json? // Лучшие сезоны
  bestTides       Json? // Лучшие приливы
  bestWeather     Json? // Лучшие погодные условия
  bestLunarPhases LunarPhaseType[] // Лучшие лунные фазы
  
  // Рейтинг
  rating        Float @default(5.0) // Общий рейтинг (1-10)
  difficulty    Int   @default(5) // Сложность (1-10)
  accessibility Int   @default(5) // Доступность (1-10)
  
  // Статистика
  totalVisits  Int   @default(0)
  successRate  Float @default(0.5) // Процент успешных рыбалок
  averageCatch Float @default(0.0) // Средний улов в кг
  
  // Дополнительно
  description String?
  tips        String[] // Советы для рыбалки
  hazards     String[] // Опасности и предостережения
  
  // Данные
  verified  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([latitude, longitude])
  @@map("fishing_hotspots")
}

// Competition and Leaderboard System Models

// Seasonal competitions and time-based tracking
model Season {
  id          String       @id @default(cuid())
  name        String       @unique
  displayName String
  description String?
  type        SeasonType
  status      SeasonStatus @default(UPCOMING)
  
  // Time period
  startDate             DateTime
  endDate               DateTime
  registrationStartDate DateTime?
  registrationEndDate   DateTime?
  
  // Configuration
  maxParticipants Int?
  minParticipants Int?     @default(1)
  isPublic        Boolean  @default(true)
  autoEnroll      Boolean  @default(false)
  allowLateJoin   Boolean  @default(true)
  
  // Categories included in this season
  includedCategories String[] @default([])
  excludedCategories String[] @default([])
  
  // Rewards and recognition
  rewards           Json?
  trophyTiers       Json?
  specialRecognition Json?
  
  // Metadata
  metadata  Json?
  createdBy String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  creator       User?                  @relation("SeasonCreator", fields: [createdBy], references: [id])
  participants  SeasonParticipant[]
  archives      SeasonArchive[]
  events        CompetitionEvent[]
  announcements SeasonAnnouncement[]
  seasonRewards Reward[]               // Награды этого сезона
  rewardCalendars RewardCalendar[]     // Календари наград
  
  @@index([type, status, startDate, endDate])
  @@index([status])
  @@map("seasons")
}

// Season participants with detailed tracking
model SeasonParticipant {
  id       String @id @default(cuid())
  seasonId String
  userId   String
  
  // Participation details
  enrolledAt   DateTime @default(now())
  isActive     Boolean  @default(true)
  autoEnrolled Boolean  @default(false)
  
  // Season performance across all categories
  totalScore    Decimal @default(0) @db.Decimal(12, 2)
  overallRank   Int?
  categoryRanks Json    @default("{}")
  categoryScores Json   @default("{}")
  
  // Progress tracking
  weeklyProgress  Json   @default("[]")
  monthlyProgress Json   @default("[]")
  achievementsEarned String[] @default([])
  badgesEarned       String[] @default([])
  
  // Historical data
  progressHistory Json @default("[]")
  rankHistory     Json @default("[]")
  
  // Season-specific stats
  seasonStats   Json?
  personalBests Json?
  
  // Metadata
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  season Season @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  user   User   @relation("SeasonParticipants", fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([seasonId, userId])
  @@index([seasonId, overallRank])
  @@map("season_participants")
}

// Historical season data archive
model SeasonArchive {
  id         String     @id @default(cuid())
  seasonId   String
  seasonName String
  seasonType SeasonType
  startDate  DateTime
  endDate    DateTime
  
  // Final results
  finalRankings    Json
  categoryWinners  Json?
  specialAwards    Json?
  participantCount Int   @default(0)
  
  // Season statistics
  seasonStats Json?
  recordsSet  Json?
  milestones  Json?
  
  // Rewards distributed
  rewardsDistributed Json?
  ceremonyData       Json?
  
  // Archive metadata
  archivedAt     DateTime @default(now())
  archivedBy     String?
  archiveVersion Int      @default(1)
  metadata       Json?
  
  // Relations
  season   Season @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  archiver User?  @relation("SeasonArchivers", fields: [archivedBy], references: [id])
  
  @@index([seasonType, startDate, endDate])
  @@map("season_archives")
}

// Scheduled competition events within seasons
model CompetitionEvent {
  id          String              @id @default(cuid())
  seasonId    String?
  title       String
  description String?
  eventType   CompetitionType     @default(SPECIAL_EVENT)
  category    CompetitionCategory
  
  // Scheduling
  scheduledStart DateTime
  scheduledEnd   DateTime
  actualStart    DateTime?
  actualEnd      DateTime?
  status         CompetitionStatus @default(UPCOMING)
  
  // Event configuration
  maxParticipants   Int?
  entryRequirements Json? // Level, achievements, etc required to join
  scoringRules      Json
  rewards           Json?
  
  // Notifications and announcements
  announcementSent Boolean @default(false)
  reminderSent     Boolean @default(false)
  resultsSent      Boolean @default(false)
  
  // Results
  winners    Json?
  results    Json?
  highlights Json?
  
  // Metadata
  createdBy String?
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  season        Season?              @relation(fields: [seasonId], references: [id])
  creator       User?                @relation("CompetitionEventCreator", fields: [createdBy], references: [id])
  announcements SeasonAnnouncement[]
  
  @@index([seasonId, scheduledStart, scheduledEnd])
  @@index([category, status])
  @@map("competition_events")
}

// Season and event announcements
model SeasonAnnouncement {
  id        String  @id @default(cuid())
  seasonId  String?
  eventId   String?
  title     String
  content   String
  type      String // 'season_start', 'event_reminder', 'results', 'awards', 'general'
  priority  String  @default("normal") // 'low', 'normal', 'high', 'urgent'
  
  // Targeting
  targetAudience String @default("all") // 'all', 'participants', 'category', 'level_range'
  targetFilters  Json?
  
  // Scheduling
  scheduledFor DateTime?
  sentAt       DateTime?
  status       String    @default("draft") // 'draft', 'scheduled', 'sent', 'cancelled'
  
  // Engagement
  recipientCount Int?     @default(0)
  openRate       Decimal? @default(0) @db.Decimal(5, 2)
  clickRate      Decimal? @default(0) @db.Decimal(5, 2)
  
  // Metadata
  createdBy String?
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  season  Season?          @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  event   CompetitionEvent? @relation(fields: [eventId], references: [id], onDelete: Cascade)
  creator User?            @relation("SeasonAnnouncementCreator", fields: [createdBy], references: [id])
  
  @@index([scheduledFor, status])
  @@index([seasonId, type])
  @@map("season_announcements")
}

// Competition categories for leaderboards
model LeaderboardCategory {
  id          String @id @default(cuid())
  name        String @unique
  displayName String
  category    CompetitionCategory
  description String?
  
  // Configuration
  isActive        Boolean @default(true)
  sortOrder       Int     @default(0)
  icon            String?
  color           String?
  
  // Scoring rules
  scoringAlgorithm Json
  updateFrequency  String @default("daily") // 'realtime', 'hourly', 'daily', 'weekly'
  
  // Visibility
  isPublic      Boolean @default(true)
  requiredLevel Int     @default(1)
  
  // Metadata
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  
  @@index([isActive, sortOrder])
  @@map("leaderboard_categories")
}

// Competitions and tournaments
model Competition {
  id          String            @id @default(cuid())
  title       String
  description String?
  category    CompetitionCategory
  type        CompetitionType   @default(MONTHLY)
  status      CompetitionStatus @default(UPCOMING)
  
  // Time period
  startDate DateTime
  endDate   DateTime
  
  // Rules and configuration
  rules            Json?
  scoringAlgorithm Json
  maxParticipants  Int?
  minParticipants  Int? @default(1)
  
  // Rewards
  rewards    Json?
  prizeTiers Json?
  
  // Visibility and privacy
  isPublic       Boolean @default(true)
  allowAnonymous Boolean @default(false)
  
  // Metadata
  metadata  Json?
  createdBy String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  creator      User?                     @relation("CompetitionCreator", fields: [createdBy], references: [id])
  participants CompetitionParticipant[]
  rankings     CompetitionRanking[]
  
  @@index([category, type])
  @@index([status, startDate, endDate])
  @@map("competitions")
}

// Competition participants
model CompetitionParticipant {
  id            String @id @default(cuid())
  competitionId String
  userId        String
  
  // Participation details
  joinedAt    DateTime @default(now())
  isActive    Boolean  @default(true)
  isAnonymous Boolean  @default(false)
  
  // Current standings
  currentScore Decimal  @default(0) @db.Decimal(10, 2)
  currentRank  Int?
  lastUpdated  DateTime @default(now())
  
  // Historical data
  scoreHistory Json @default("[]")
  rankHistory  Json @default("[]")
  
  // Metadata
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relations
  competition Competition @relation(fields: [competitionId], references: [id], onDelete: Cascade)
  user        User        @relation("CompetitionParticipants", fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([competitionId, userId])
  @@index([competitionId, currentRank])
  @@map("competition_participants")
}

// Competition rankings snapshots
model CompetitionRanking {
  id            String @id @default(cuid())
  competitionId String
  userId        String
  rank          Int
  score         Decimal @db.Decimal(10, 2)
  details       Json? // Category-specific details
  
  // Snapshot info
  snapshotDate     DateTime @default(now())
  isCurrentRanking Boolean  @default(true)
  
  // Metadata
  metadata  Json?
  createdAt DateTime @default(now())
  
  // Relations
  competition Competition @relation(fields: [competitionId], references: [id], onDelete: Cascade)
  user        User        @relation("CompetitionRankings", fields: [userId], references: [id], onDelete: Cascade)
  
  @@index([competitionId, isCurrentRanking])
  @@index([competitionId, rank])
  @@map("competition_rankings")
}

// Приливы и отливы
model TidalData {
  id String @id @default(cuid())
  
  // Время и место
  datetime DateTime // Время прилива/отлива
  location Json // Координаты
  
  // Данные о приливе
  type     TideType // Прилив или отлив
  height   Float // Высота в метрах
  strength Int // Сила (1-10)
  
  // Влияние на рыбалку
  fishingImpact FishingImpact // Влияние на клёв
  bestSpecies   FishSpecies[] // Виды, наиболее активные в это время
  
  createdAt DateTime @default(now())
  
  @@index([datetime])
  @@index([location])
  @@map("tidal_data")
}

enum TideType {
  HIGH_TIDE // Прилив
  LOW_TIDE // Отлив
}

enum FishingImpact {
  VERY_NEGATIVE
  NEGATIVE
  NEUTRAL
  POSITIVE
  VERY_POSITIVE
}

// Историческая статистика по лунным фазам
model LunarFishingStats {
  id String @id @default(cuid())
  
  // Параметры
  lunarPhase LunarPhaseType
  species    FishSpecies
  location   Json // Регион/координаты
  season     Int // Сезон (1-4)
  
  // Статистика
  totalObservations Int // Количество наблюдений
  averageSuccess    Float // Средняя успешность
  averageCatch      Float // Средний улов в кг
  bestHours         Json? // Лучшие часы дня
  
  // Корреляция
  correlationStrength Float // Сила корреляции с лунной фазой (-1 to 1)
  confidence          Float // Уверенность в данных
  
  // Обновление данных
  lastCalculated DateTime // Когда последний раз рассчитывалось
  dataPoints     Int // Количество точек данных
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([lunarPhase, species, location, season])
  @@map("lunar_fishing_stats")
}

// ==== ЦИФРОВОЙ ДНЕВНИК РЫБОЛОВА ====

// Основная запись дневника рыболова
model FishingDiaryEntry {
  id String @id @default(cuid())

  // Основная информация
  userId      String
  title       String // Заголовок записи
  description String? // Описание рыбалки
  date        DateTime // Дата рыбалки

  // Локация с GPS
  locationName String? // Название места
  latitude     Float? // GPS координаты
  longitude    Float?
  accuracy     Float? // Точность GPS (в метрах)

  // Детали рыбалки  
  weather       Json? // Погодные условия
  temperature   Float? // Температура воды/воздуха
  windSpeed     Float? // Скорость ветра
  windDirection WindDirection? // Направление ветра

  // Улов
  fishCaught  DiaryFishCatch[] // Пойманная рыба
  totalWeight Float? // Общий вес улова
  totalCount  Int              @default(0) // Общее количество рыбы

  // Снасти и приманки
  rodType   String? // Тип удилища
  reelType  String? // Тип катушки
  lineType  String? // Тип лески
  baitUsed  String[] @default([]) // Использованные приманки
  lureColor String? // Цвет приманки

  // Медиа
  media DiaryMedia[] // Фото и видео

  // Метки и категории
  tags      String[] @default([]) // Теги
  isPrivate Boolean  @default(false) // Приватная запись
  rating    Int? // Личная оценка (1-5)

  // Связи
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, date])
  @@index([latitude, longitude])
  @@map("fishing_diary_entries")
}

// Пойманная рыба в записи дневника
model DiaryFishCatch {
  id      String @id @default(cuid())
  entryId String

  // Информация о рыбе
  species  FishSpecies // Вид рыбы
  weight   Float? // Вес в кг
  length   Float? // Длина в см
  quantity Int         @default(1) // Количество

  // Детали поимки
  timeOfCatch DateTime? // Точное время поимки
  depth       Float? // Глубина ловли
  method      FishingTechnique? // Способ ловли
  baitUsed    String? // Конкретная приманка

  // Дополнительно
  wasReleased Boolean @default(false) // Была ли отпущена
  notes       String? // Заметки о поимке

  // Связи
  entry FishingDiaryEntry @relation(fields: [entryId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([entryId])
  @@index([species])
  @@map("diary_fish_catches")
}

// Медиафайлы дневника (фото/видео)
model DiaryMedia {
  id      String @id @default(cuid())
  entryId String

  // Файл
  fileName  String // Имя файла
  fileUrl   String // URL файла
  fileSize  Int? // Размер файла в байтах
  mimeType  String // MIME тип файла
  mediaType DiaryMediaType // Тип медиа

  // EXIF метаданные (извлеченные из фото)
  exifData     Json? // Все EXIF данные
  gpsLatitude  Float? // GPS из EXIF
  gpsLongitude Float? // GPS из EXIF
  captureTime  DateTime? // Время съемки из EXIF
  cameraModel  String? // Модель камеры
  lensModel    String? // Модель объектива

  // Дополнительная информация
  title       String? // Заголовок медиа
  description String? // Описание
  isPublic    Boolean @default(true) // Публичное ли фото

  // Связи
  entry FishingDiaryEntry @relation(fields: [entryId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([entryId])
  @@index([mediaType])
  @@map("diary_media")
}

// ==== УМНЫЕ РЕКОМЕНДАЦИИ ====

// Основная модель для хранения умных рекомендаций
model SmartRecommendation {
  id String @id @default(cuid())

  // Тип рекомендации
  type RecommendationType // Тип: история, погода, социальная

  // Целевая аудитория
  targetUserId String? // Персональная рекомендация для конкретного пользователя
  targetUserRole UserRole[] @default([]) // Или для группы ролей
  skillLevel SkillLevelRequired[] @default([]) // Уровень навыков

  // Содержание рекомендации
  title String // Заголовок рекомендации
  description String // Описание
  aiGeneratedText String? // Текст, сгенерированный AI
  
  // Контекст для рекомендации
  triggerContext Json? // Контекст, который вызвал рекомендацию
  weatherConditions Json? // Погодные условия
  
  // Связанные объекты
  recommendedTripId String? // ID рекомендуемой поездки
  recommendedTrip GroupTrip? @relation(fields: [recommendedTripId], references: [id], onDelete: Cascade)
  
  recommendedSpecies FishSpecies[] @default([]) // Рекомендуемые виды рыб
  recommendedTechniques FishingTechnique[] @default([]) // Рекомендуемые техники
  recommendedLocation Json? // Координаты места
  
  // Капитан, давший рекомендацию (для социальных)
  fromCaptainId String?
  fromCaptain User? @relation("CaptainRecommendations", fields: [fromCaptainId], references: [id], onDelete: SetNull)

  // Приоритет и релевантность
  priority Int @default(5) // Приоритет (1-10)
  relevanceScore Float @default(0.5) // Оценка релевантности AI (0-1)
  confidenceScore Float @default(0.5) // Уверенность в рекомендации (0-1)

  // Статистика эффективности
  impressions Int @default(0) // Показы
  clicks Int @default(0) // Клики
  conversions Int @default(0) // Конверсии (действия пользователей)
  
  // Временные ограничения
  validFrom DateTime @default(now()) // Действительна с
  validUntil DateTime? // Действительна до
  
  // Статус и метаданные
  isActive Boolean @default(true)
  metadata Json? // Дополнительные данные
  
  // Связи
  targetUser User? @relation("UserRecommendations", fields: [targetUserId], references: [id], onDelete: Cascade)
  interactions RecommendationInteraction[] // Взаимодействия
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([type])
  @@index([targetUserId])
  @@index([isActive, validFrom, validUntil])
  @@map("smart_recommendations")
}

// Взаимодействия пользователей с рекомендациями
model RecommendationInteraction {
  id String @id @default(cuid())
  
  userId String
  recommendationId String
  
  // Тип взаимодействия
  interactionType RecommendationInteractionType
  
  // Контекст
  sessionId String? // ID сессии
  deviceType String? // Тип устройства
  userAgent String? // User agent
  
  // Результат взаимодействия
  result Json? // Результат действия (например, забронированная поездка)
  satisfaction Int? // Оценка удовлетворенности (1-5)
  feedback String? // Обратная связь
  
  // Связи
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  recommendation SmartRecommendation @relation(fields: [recommendationId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  
  @@index([userId])
  @@index([recommendationId])
  @@index([interactionType])
  @@map("recommendation_interactions")
}

// Погодные рекомендации от AI
model WeatherRecommendation {
  id String @id @default(cuid())
  
  // Погодные условия
  weatherConditions Json // Детальные погодные данные
  windSpeed Float? // Скорость ветра
  windDirection WindDirection?
  temperature Float? // Температура
  pressure Float? // Давление
  humidity Float? // Влажность
  cloudCover Float? // Облачность (0-1)
  
  // Рекомендации
  recommendedSpecies FishSpecies[] // Какая рыба лучше клюет
  recommendedTechniques FishingTechnique[] // Какие техники использовать
  recommendedTimeOfDay Json? // Лучшее время дня [start_hour, end_hour]
  recommendedDepth Float? // Рекомендуемая глубина
  
  // AI анализ
  aiAnalysis String // Анализ от OpenAI
  aiReasoning String? // Объяснение логики
  confidenceLevel Float @default(0.5) // Уверенность AI (0-1)
  
  // Локация
  location Json? // Координаты
  regionId String? // ID региона
  
  // Валидность
  validFor DateTime // До какого времени действительна
  
  // Статистика
  usageCount Int @default(0) // Сколько раз использовалась
  successRate Float @default(0.0) // Процент успешных исходов
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([validFor])
  @@index([location])
  @@map("weather_recommendations")
}

// Рекомендации от капитанов
model CaptainRecommendation {
  id String @id @default(cuid())
  
  captainId String
  
  // Содержание рекомендации
  title String
  content String // Основной текст рекомендации
  category CaptainRecommendationCategory
  
  // Целевая аудитория
  targetSkillLevel SkillLevelRequired[] @default([])
  targetSpecies FishSpecies[] @default([]) // Для каких видов рыб
  targetTechniques FishingTechnique[] @default([]) // Для каких техник
  
  // Контекст
  seasonalContext Int[] @default([]) // Месяцы (1-12)
  weatherContext Json? // При каких погодных условиях
  locationContext Json? // Для каких мест
  
  // Связанные поездки
  relatedTripIds String[] @default([]) // ID связанных поездок
  
  // Авторитетность и проверка
  isVerified Boolean @default(false) // Проверена ли администрацией
  endorsements Int @default(0) // Количество одобрений
  
  // Статистика
  views Int @default(0)
  helpfulVotes Int @default(0) // Голоса "полезно"
  notHelpfulVotes Int @default(0) // Голоса "не полезно"
  
  // Статус
  isActive Boolean @default(true)
  moderationStatus ModerationStatus @default(PENDING)
  
  // Связи
  captain User @relation("CaptainRecommendationsAuthored", fields: [captainId], references: [id], onDelete: Cascade)
  votes CaptainRecommendationVote[] // Голоса за эту рекомендацию
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([captainId])
  @@index([category])
  @@index([isActive, moderationStatus])
  @@map("captain_recommendations")
}

// Голосование за рекомендации капитанов
model CaptainRecommendationVote {
  id String @id @default(cuid())
  
  userId String
  recommendationId String
  
  isHelpful Boolean // true = helpful, false = not helpful
  
  // Связи
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  recommendation CaptainRecommendation @relation(fields: [recommendationId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([userId, recommendationId])
  @@index([recommendationId])
  @@map("captain_recommendation_votes")
}

// Похожесть поездок для collaborative filtering
model TripSimilarity {
  id String @id @default(cuid())
  
  tripId1 String
  tripId2 String
  
  // Метрики похожести
  overallSimilarity Float // Общая похожесть (0-1)
  
  // Компоненты похожести
  speciesSimilarity Float @default(0) // По видам рыб
  techniqueSimilarity Float @default(0) // По техникам
  locationSimilarity Float @default(0) // По местам
  skillSimilarity Float @default(0) // По уровню навыков
  seasonSimilarity Float @default(0) // По сезону/времени
  participantSimilarity Float @default(0) // По участникам
  
  // Метаданные расчета
  calculatedAt DateTime @default(now()) // Когда рассчитано
  calculationMethod String // Метод расчета
  isStale Boolean @default(false) // Устарел ли расчет
  
  // Связи
  trip1 GroupTrip @relation("Trip1Similarities", fields: [tripId1], references: [id], onDelete: Cascade)
  trip2 GroupTrip @relation("Trip2Similarities", fields: [tripId2], references: [id], onDelete: Cascade)
  
  @@unique([tripId1, tripId2])
  @@index([overallSimilarity])
  @@index([calculatedAt])
  @@map("trip_similarities")
}

// ==== НОВЫЕ ENUMS ====

enum WindDirection {
  NORTH
  NORTHEAST
  EAST
  SOUTHEAST
  SOUTH
  SOUTHWEST
  WEST
  NORTHWEST
  CALM

  @@map("wind_direction")
}

enum DiaryMediaType {
  PHOTO
  VIDEO
  AUDIO

  @@map("diary_media_type")
}

enum RecommendationType {
  HISTORY_BASED // На основе истории похожих поездок
  WEATHER_AI // Погодные AI рекомендации
  SOCIAL_CAPTAIN // Социальные рекомендации от капитанов
  COLLABORATIVE // Коллаборативная фильтрация
  CONTENT_BASED // На основе контента
  HYBRID // Гибридные рекомендации
  
  @@map("recommendation_type")
}

enum RecommendationInteractionType {
  VIEW // Просмотр
  CLICK // Клик по рекомендации
  LIKE // Лайк
  DISLIKE // Дизлайк
  SHARE // Поделился
  BOOK // Забронировал рекомендованную поездку
  DISMISS // Отклонил рекомендацию
  REPORT // Пожаловался на рекомендацию
  
  @@map("recommendation_interaction_type")
}

enum CaptainRecommendationCategory {
  TECHNIQUE_ADVICE // Советы по техникам
  SPECIES_TARGETING // Советы по видам рыб
  LOCATION_TIPS // Советы по местам
  EQUIPMENT_RECOMMENDATION // Рекомендации снаряжения
  WEATHER_STRATEGY // Стратегии для погоды
  SAFETY_ADVICE // Советы по безопасности
  BEGINNER_GUIDE // Руководство для новичков
  SEASONAL_TIPS // Сезонные советы
  
  @@map("captain_recommendation_category")
}

enum ModerationStatus {
  PENDING // Ожидает модерации
  APPROVED // Одобрено
  REJECTED // Отклонено
  FLAGGED // Помечено для проверки
  
  @@map("moderation_status")
}

// ============================================================================
// СИСТЕМА ВЫПЛАТ (PAYOUT SYSTEM)
// ============================================================================

// Графики выплат для капитанов
model Payout {
  id String @id @default(cuid())
  
  // Связь с капитаном
  captainId String
  captain   User   @relation("CaptainPayouts", fields: [captainId], references: [id], onDelete: Cascade)
  
  // Финансовые данные
  amount               Int      // Сумма выплаты в центах
  currency             String   @default("EUR")
  commissionAmount     Int      // Сумма комиссии, которая выплачивается
  commissionRate       Float    // Процент комиссии на момент выплаты
  
  // Период выплаты
  periodStart          DateTime // Начало периода
  periodEnd            DateTime // Конец периода
  
  // Статус и обработка
  status               PayoutStatus @default(PENDING)
  scheduleType         PayoutScheduleType @default(MONTHLY)
  
  // Платежная информация
  paymentMethodId      String?  // ID способа оплаты капитана
  paymentMethod        PaymentMethod? @relation(fields: [paymentMethodId], references: [id], onDelete: SetNull)
  
  // Stripe данные (если используется Stripe Connect)
  stripeTransferId     String?  @unique
  stripeAccountId      String?  // Connected Account ID капитана
  
  // Банковские реквизиты (если не используется Stripe)
  bankAccountNumber    String?
  bankRoutingNumber    String?
  bankName             String?
  bankAccountHolderName String?
  
  // Обработка и подтверждение
  processedAt          DateTime? // Когда обработана
  processedBy          String?   // Кто обработал (admin user ID)
  approvedAt           DateTime? // Когда одобрена
  approvedBy           String?   // Кто одобрил
  rejectedAt           DateTime? // Когда отклонена
  rejectedBy           String?   // Кто отклонил
  rejectionReason      String?   // Причина отклонения
  
  // Уведомления
  notificationSent     Boolean   @default(false)
  remindersSent        Int       @default(0) // Количество отправленных напоминаний
  lastReminderAt       DateTime?
  
  // Налоговая отчетность
  taxYear              Int?      // Налоговый год
  taxDocumentGenerated Boolean   @default(false)
  taxDocumentUrl       String?   // URL налогового документа
  
  // Метаданные
  description          String?   // Описание выплаты
  notes                String?   // Внутренние заметки
  metadata             Json?     // Дополнительные данные
  
  // Связанные платежи
  relatedPaymentIds    String[]  @default([]) // ID платежей, включенных в выплату
  
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt
  
  @@index([captainId, status])
  @@index([periodStart, periodEnd])
  @@index([status, scheduleType])
  @@map("payouts")
}

// Настройки графика выплат для капитанов
model PayoutSchedule {
  id String @id @default(cuid())
  
  // Связь с капитаном
  captainId String @unique
  captain   User   @relation("CaptainPayoutSchedule", fields: [captainId], references: [id], onDelete: Cascade)
  
  // Настройки графика
  scheduleType         PayoutScheduleType @default(MONTHLY)
  isActive             Boolean            @default(true)
  
  // Минимальная сумма для выплаты
  minimumPayoutAmount  Int                @default(5000) // €50 минимум
  
  // Автоматические выплаты
  autoPayoutEnabled    Boolean            @default(false)
  
  // День выплаты
  payoutDay            Int?               // День месяца (1-31) для месячных, день недели (1-7) для еженедельных
  
  // Способ оплаты по умолчанию
  defaultPaymentMethodId String?
  defaultPaymentMethod   PaymentMethod? @relation(fields: [defaultPaymentMethodId], references: [id], onDelete: SetNull)
  
  // Банковские реквизиты по умолчанию
  defaultBankAccount   Json?              // Банковские данные по умолчанию
  
  // Уведомления
  notifyBeforePayoutDays Int              @default(3) // За сколько дней уведомлять
  emailNotifications     Boolean          @default(true)
  smsNotifications       Boolean          @default(false)
  
  // Налоговые настройки
  taxSettings          Json?              // Налоговые настройки и документы
  
  createdAt            DateTime           @default(now())
  updatedAt            DateTime           @updatedAt
  
  @@map("payout_schedules")
}

// История изменений выплат для аудита
model PayoutHistoryLog {
  id String @id @default(cuid())
  
  payoutId String
  
  // Изменения
  action         PayoutAction  // Тип действия
  previousStatus PayoutStatus? // Предыдущий статус
  newStatus      PayoutStatus? // Новый статус
  
  // Кто и когда
  performedBy    String        // User ID
  performedAt    DateTime      @default(now())
  
  // Детали изменения
  changeDetails  Json?         // Подробности изменения
  reason         String?       // Причина изменения
  
  // IP и device tracking для безопасности
  ipAddress      String?
  userAgent      String?
  
  createdAt      DateTime      @default(now())
  
  @@index([payoutId])
  @@index([performedAt])
  @@map("payout_history_logs")
}

enum PayoutStatus {
  PENDING      // Ожидает обработки
  APPROVED     // Одобрена для выплаты
  PROCESSING   // Обрабатывается
  COMPLETED    // Выполнена успешно
  FAILED       // Неуспешная выплата
  CANCELLED    // Отменена
  ON_HOLD      // Приостановлена
}

enum PayoutScheduleType {
  WEEKLY       // Еженедельно
  BIWEEKLY     // Раз в две недели
  MONTHLY      // Ежемесячно
  QUARTERLY    // Ежеквартально
  MANUAL       // Вручную по запросу
}

enum PayoutAction {
  CREATED      // Создана
  APPROVED     // Одобрена
  REJECTED     // Отклонена
  PROCESSED    // Обработана
  COMPLETED    // Завершена
  FAILED       // Неудачная
  CANCELLED    // Отменена
  UPDATED      // Обновлена
}

// ============================================================================
// НАЛОГОВАЯ ОТЧЕТНОСТЬ (TAX REPORTING SYSTEM)
// ============================================================================

// Основная модель налогового отчета (годовой/квартальный)
model TaxReport {
  id String @id @default(cuid())
  
  // Период отчета
  taxYear        Int         // Налоговый год
  reportType     TaxReportType // Тип отчета (годовой, квартальный)
  quarterNumber  Int?        // Номер квартала (1-4), если квартальный отчет
  
  // Период данных
  periodStart    DateTime    // Начало отчетного периода
  periodEnd      DateTime    // Конец отчетного периода
  
  // Статус отчета
  status         TaxReportStatus @default(DRAFT)
  
  // Финансовые итоги
  totalGrossCommissions    Int      // Общая сумма комиссий до вычетов (центы)
  totalNetCommissions      Int      // Общая чистая сумма комиссий (центы)
  totalWithholdings        Int      // Общие удержания (центы)
  totalPayouts             Int      // Общие выплаты (центы)
  
  // Количественные показатели
  totalCaptains            Int      // Количество капитанов
  totalTransactions        Int      // Количество транзакций
  totalDocumentsGenerated  Int      // Количество созданных документов
  
  // Генерация отчета
  generatedAt    DateTime?   // Когда сгенерирован
  generatedBy    String?     // Кто сгенерировал (admin user ID)
  
  // Файлы отчета
  reportFileUrl       String?     // URL основного файла отчета
  summaryFileUrl      String?     // URL сводного файла
  backupFileUrl       String?     // URL резервной копии
  
  // Статистика и аналитика
  reportSummary      Json?        // Сводная статистика отчета
  complianceChecks   Json?        // Результаты проверок соответствия
  
  // Аудит и подписи
  approvedAt         DateTime?    // Когда утвержден
  approvedBy         String?      // Кто утвердил
  digitalSignature   String?      // Цифровая подпись
  
  // Связи
  taxDocuments       TaxDocument[] // Связанные налоговые документы
  auditLogs          TaxAuditLog[] // Аудиторские записи
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([taxYear, reportType, quarterNumber])
  @@index([taxYear, status])
  @@index([periodStart, periodEnd])
  @@map("tax_reports")
}

// Налоговые документы (1099-MISC и другие)
model TaxDocument {
  id String @id @default(cuid())
  
  // Связи
  taxReportId    String?     // Связь с отчетом (может быть независимым)
  taxReport      TaxReport?  @relation(fields: [taxReportId], references: [id], onDelete: SetNull)
  
  captainId      String      // Для кого документ
  captain        User        @relation("CaptainTaxDocuments", fields: [captainId], references: [id], onDelete: Cascade)
  
  // Тип и характеристики документа
  documentType   TaxDocumentType   // Тип документа (1099-MISC, etc.)
  formType       String            // Форма документа ("1099-MISC", "1099-K", etc.)
  
  // Налоговый период
  taxYear        Int               // Налоговый год
  
  // Финансовые данные документа
  totalAmount    Int               // Общая сумма в центах
  
  // Детализация сумм (специфично для 1099-MISC)
  box1Amount     Int?             // Box 1: Rents
  box2Amount     Int?             // Box 2: Royalties  
  box3Amount     Int?             // Box 3: Other Income
  box4Amount     Int?             // Box 4: Federal Income Tax Withheld
  box5Amount     Int?             // Box 5: Fishing Boat Proceeds
  box6Amount     Int?             // Box 6: Medical and Health Care Payments
  box7Amount     Int?             // Box 7: Nonemployee Compensation (основное для нас)
  box8Amount     Int?             // Box 8: Substitute Payments
  box9Amount     Int?             // Box 9: Direct Sales
  box10Amount    Int?             // Box 10: Crop Insurance Proceeds
  box11Amount    Int?             // Box 11: Foreign Tax Paid
  box12Amount    Int?             // Box 12: Section 409A Deferrals
  box13Amount    Int?             // Box 13: Excess Golden Parachute Payments
  box14Amount    Int?             // Box 14: Gross Proceeds Paid to Attorney
  
  // Статус документа
  status         TaxDocumentStatus @default(DRAFT)
  
  // Генерация и отправка
  generatedAt    DateTime?        // Когда сгенерирован
  sentAt         DateTime?        // Когда отправлен получателю
  receivedAt     DateTime?        // Когда получен подтверждение
  
  // Файлы документа
  documentUrl    String?          // URL PDF файла
  xmlDataUrl     String?          // URL XML данных (для электронной подачи)
  backupUrl      String?          // URL резервной копии
  
  // Получатель информации
  recipientTin   String?          // TIN получателя (Tax Identification Number)
  recipientName  String           // Полное имя получателя
  recipientAddress Json?          // Адрес получателя
  
  // Плательщик (Cascais Fishing)
  payerTin       String?          // TIN плательщика  
  payerName      String           // Название компании
  payerAddress   Json?            // Адрес компании
  
  // Соответствие и валидация
  isValidated    Boolean          @default(false) // Прошел ли валидацию
  validationErrors Json?          // Ошибки валидации
  complianceFlags  String[]       @default([])    // Флаги соответствия
  
  // Коррекции и исправления
  isCorrection   Boolean          @default(false) // Является ли исправлением
  originalDocId  String?          // ID оригинального документа
  correctionReason String?        // Причина исправления
  
  // Аудит и отслеживание
  auditLogs      TaxAuditLog[]
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([captainId, taxYear, documentType])
  @@index([taxYear, status])
  @@index([captainId, taxYear])
  @@index([generatedAt])
  @@map("tax_documents")
}

// Аудиторский журнал для налогового соответствия
model TaxAuditLog {
  id String @id @default(cuid())
  
  // Связи (одна из связей должна быть обязательной)
  taxReportId     String?
  taxReport       TaxReport?   @relation(fields: [taxReportId], references: [id], onDelete: SetNull)
  
  taxDocumentId   String?
  taxDocument     TaxDocument? @relation(fields: [taxDocumentId], references: [id], onDelete: SetNull)
  
  payoutId        String?     // Связь с выплатой, если применимо
  
  // Действие и событие
  action          TaxAuditAction    // Тип действия
  eventType       TaxEventType      // Тип события
  
  // Пользователь и контекст
  performedBy     String            // User ID кто выполнил
  performedByName String?           // Имя пользователя для аудита
  
  // Детали события
  description     String            // Описание действия
  previousData    Json?             // Предыдущие данные
  newData         Json?             // Новые данные
  
  // Техническая информация
  ipAddress       String?           // IP адрес
  userAgent       String?           // User agent
  sessionId       String?           // ID сессии
  
  // Соответствие и риски
  complianceLevel TaxComplianceLevel @default(NORMAL)
  riskFlags       String[]           @default([])
  
  // Время
  performedAt DateTime @default(now())
  
  @@index([taxReportId])
  @@index([taxDocumentId])
  @@index([performedBy])
  @@index([performedAt])
  @@index([action, eventType])
  @@map("tax_audit_logs")
}

// Enum типы для налоговой системы

enum TaxReportType {
  ANNUAL     // Годовой отчет
  QUARTERLY  // Квартальный отчет
  MONTHLY    // Месячный отчет (если нужен)
  CUSTOM     // Произвольный период
}

enum TaxReportStatus {
  DRAFT      // Черновик
  PROCESSING // Обрабатывается
  COMPLETED  // Завершен
  SUBMITTED  // Отправлен
  APPROVED   // Утвержден
  REJECTED   // Отклонен
  ARCHIVED   // Архивирован
}

enum TaxDocumentType {
  FORM_1099_MISC    // 1099-MISC форма
  FORM_1099_K       // 1099-K форма
  FORM_1096         // 1096 Summary форма
  ANNUAL_SUMMARY    // Годовая сводка
  QUARTERLY_SUMMARY // Квартальная сводка
  CORRECTION        // Исправление
  CUSTOM            // Произвольный документ
}

enum TaxDocumentStatus {
  DRAFT         // Черновик
  GENERATED     // Сгенерирован
  VALIDATED     // Проверен
  APPROVED      // Утвержден
  SENT          // Отправлен
  RECEIVED      // Получен
  FILED         // Подан
  CORRECTED     // Исправлен
  CANCELLED     // Отменен
}

enum TaxAuditAction {
  CREATED       // Создан
  UPDATED       // Обновлен
  DELETED       // Удален
  GENERATED     // Сгенерирован
  SENT          // Отправлен
  APPROVED      // Утвержден
  REJECTED      // Отклонен
  VALIDATED     // Проверен
  CORRECTED     // Исправлен
  ARCHIVED      // Архивирован
  ACCESSED      // Доступ осуществлен
  DOWNLOADED    // Скачан
  EXPORTED      // Экспортирован
}

enum TaxEventType {
  REPORT_MANAGEMENT    // Управление отчетами
  DOCUMENT_MANAGEMENT  // Управление документами
  DATA_EXPORT          // Экспорт данных
  COMPLIANCE_CHECK     // Проверка соответствия
  USER_ACCESS          // Доступ пользователя
  SYSTEM_MAINTENANCE   // Обслуживание системы
  AUDIT_REVIEW         // Аудиторская проверка
  CORRECTION_PROCESS   // Процесс исправления
  NOTIFICATION_SENT    // Отправка уведомления
}

enum TaxComplianceLevel {
  LOW       // Низкий уровень риска
  NORMAL    // Обычный уровень
  HIGH      // Высокий уровень риска
  CRITICAL  // Критический уровень
}

// Privacy controls for leaderboards and rankings
enum LeaderboardVisibility {
  PUBLIC      // Виден всем в лидербордах
  FRIENDS     // Виден только друзьям
  PRIVATE     // Не виден никому в лидербордах
  ANONYMOUS   // Виден анонимно (без имени)
}

enum ProfileVisibility {
  PUBLIC      // Профиль виден всем
  FRIENDS     // Профиль виден только друзьям  
  PRIVATE     // Профиль приватный
  LIMITED     // Ограниченная видимость (только базовые данные)
}

// ============================================================================
// СИСТЕМА НАГРАД И РАСПРЕДЕЛЕНИЯ (REWARD DISTRIBUTION SYSTEM)
// ============================================================================

// Основные награды в системе
model Reward {
  id          String      @id @default(cuid())
  name        String      // Название награды
  description String?     // Описание награды
  type        RewardType  // Тип награды
  tier        RewardTier  // Уровень награды (bronze, silver, gold, platinum)
  
  // Визуальное оформление
  icon        String      // Иконка награды
  color       String?     // Цвет награды
  imageUrl    String?     // URL изображения награды
  
  // Критерии получения
  requirements Json?      // Требования для получения
  rarity      AchievementRarity @default(COMMON)
  
  // Статус и даты
  isActive    Boolean     @default(true)
  isLimited   Boolean     @default(false) // Ограниченное количество
  maxQuantity Int?        // Максимальное количество экземпляров
  
  // Сезонность
  seasonId    String?     // Привязка к сезону
  validFrom   DateTime?   // Действительна с
  validUntil  DateTime?   // Действительна до
  
  // Метаданные
  metadata    Json?       // Дополнительные данные
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  
  // Связи
  season          Season?             @relation(fields: [seasonId], references: [id])
  distributions   RewardDistribution[] // Раздачи этой награды
  inventory       RewardInventory[]   // В инвентарях пользователей
  events          RewardEvent[]       // События награждения
  
  @@map("rewards")
}

// Распределение наград (кто, что, когда получил)
model RewardDistribution {
  id          String                @id @default(cuid())
  rewardId    String
  userId      String
  
  // Источник награды
  sourceType  RewardSourceType      // Откуда награда (competition, achievement, etc)
  sourceId    String?               // ID источника (competitionId, achievementId, etc)
  
  // Детали распределения
  rank        Int?                  // Место в соревновании (если применимо)
  score       Decimal?              @db.Decimal(10, 2) // Балл/результат
  reason      String?               // Причина награждения
  
  // Статус
  status      RewardDistributionStatus @default(PENDING)
  distributedAt DateTime?            // Когда была выдана
  claimedAt   DateTime?             // Когда была получена пользователем
  
  // Уведомления
  notificationSent Boolean          @default(false)
  ceremonyShown   Boolean           @default(false)
  
  // Метаданные
  metadata    Json?
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt
  
  // Связи
  reward      Reward                @relation(fields: [rewardId], references: [id])
  user        User                  @relation(fields: [userId], references: [id])
  
  @@index([userId])
  @@index([sourceType, sourceId])
  @@map("reward_distributions")
}

// Инвентарь наград пользователя
model RewardInventory {
  id          String    @id @default(cuid())
  userId      String
  rewardId    String
  
  // Количество и статус
  quantity    Int       @default(1)     // Количество экземпляров
  isActive    Boolean   @default(true)  // Активна ли награда
  isDisplayed Boolean   @default(true)  // Отображается ли в профиле
  
  // Порядок отображения
  displayOrder Int?     // Порядок в профиле
  category    String?   // Категория для группировки
  
  // Получение
  obtainedAt  DateTime  @default(now()) // Когда получена
  firstObtainedAt DateTime @default(now()) // Когда получена впервые
  
  // Связи
  reward      Reward    @relation(fields: [rewardId], references: [id])
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([userId, rewardId])
  @@index([userId, isActive])
  @@map("reward_inventory")
}

// События награждения (для церемоний и уведомлений)
model RewardEvent {
  id          String         @id @default(cuid())
  rewardId    String
  
  // Тип и контекст события
  eventType   RewardEventType // Тип события (ceremony, notification, etc)
  title       String         // Заголовок события
  description String?        // Описание события
  
  // Участники
  participants String[]      // ID пользователей-участников
  winnerId    String?        // ID главного победителя
  
  // Расписание
  scheduledFor DateTime?     // Запланировано на
  startedAt   DateTime?      // Когда началось
  completedAt DateTime?      // Когда завершилось
  
  // Статус
  status      RewardEventStatus @default(SCHEDULED)
  isPublic    Boolean        @default(true)
  
  // Контент события
  bannerUrl   String?        // Баннер события
  messageContent Json?       // Содержание сообщения
  
  // Связи
  reward      Reward         @relation(fields: [rewardId], references: [id])
  winner      User?          @relation("RewardEventWinner", fields: [winnerId], references: [id])
  
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  
  @@index([eventType, scheduledFor])
  @@map("reward_events")
}

// Сезонные календари наград
model RewardCalendar {
  id          String    @id @default(cuid())
  name        String    // Название календаря
  description String?   // Описание
  
  // Период действия
  startDate   DateTime  // Начало календаря
  endDate     DateTime  // Конец календаря
  
  // Конфигурация
  rewards     Json      // Награды и правила их получения
  milestones  Json?     // Этапы и промежуточные цели
  
  // Статус
  isActive    Boolean   @default(true)
  isPublic    Boolean   @default(true)
  
  // Связь с сезоном
  seasonId    String?
  season      Season?   @relation(fields: [seasonId], references: [id])
  
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  
  @@map("reward_calendars")
}

// Enums для системы наград
enum RewardType {
  TROPHY      // Трофей
  BADGE       // Значок
  TITLE       // Титул
  DECORATION  // Украшение профиля
  FEATURE     // Эксклюзивная функция
  VIRTUAL_ITEM // Виртуальный предмет
}

enum RewardTier {
  BRONZE      // Бронза
  SILVER      // Серебро
  GOLD        // Золото
  PLATINUM    // Платина
  DIAMOND     // Алмаз
  LEGENDARY   // Легендарный
}

enum RewardSourceType {
  COMPETITION       // Соревнование
  ACHIEVEMENT       // Достижение
  MILESTONE        // Веха
  SEASONAL_EVENT   // Сезонное событие
  SPECIAL_EVENT    // Специальное событие
  ADMIN_GRANT      // Выдано администратором
  COMMUNITY_VOTE   // Голосование сообщества
}

enum RewardDistributionStatus {
  PENDING     // Ожидает распределения
  DISTRIBUTED // Распределена
  CLAIMED     // Получена пользователем
  EXPIRED     // Истекла
  CANCELLED   // Отменена
}

enum RewardEventType {
  CEREMONY    // Церемония награждения
  NOTIFICATION // Уведомление
  ANNOUNCEMENT // Объявление
  MILESTONE   // Достижение вехи
}

enum RewardEventStatus {
  SCHEDULED   // Запланировано
  ACTIVE      // Активно
  COMPLETED   // Завершено
  CANCELLED   // Отменено
}
